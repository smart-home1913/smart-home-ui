export enum ConditionType {
  BY_VALUE = "by_value",
  BY_TRIGGER = "by_trigger",
}

export enum Operator {
  GREATER_THEN = ">",
  LESS_THEN = "<",
  LESS_EQUAL_THEN = "<=",
  GREATER_EQUAL_THEN = ">=",
  EQUAL = "==",
  NOT_EQUAL = "!=",
}

export enum ValueType {
  NUMBER = "number",
  BOOLEAN = "boolean",
}
